//! Relocated to `atlas_custos::ports::security_audit` (S2d). Re-exported here
//! to keep every existing `crate::ports::security_audit::SecurityAuditRepo`
//! import path compiling.

pub use atlas_custos::ports::security_audit::SecurityAuditRepo;

//! Relocated to `atlas_custos::ports::group_repo` (S2d). Re-exported here to
//! keep every existing `crate::ports::group_repo::GroupRepo` import path
//! compiling.

pub use atlas_custos::ports::group_repo::GroupRepo;
